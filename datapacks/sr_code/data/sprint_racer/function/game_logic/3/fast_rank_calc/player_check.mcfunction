execute if score @s KOs > #bestKOcount value run scoreboard players operation #bestKOcount value = @s KOs

#why's this in its own function?

#so things don't break
#don't ask me why, this just how it be